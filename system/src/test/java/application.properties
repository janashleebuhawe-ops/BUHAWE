spring.application.name=system
server.port=${PORT:8080}

# Database
spring.datasource.url=jdbc:mysql://${MYSQLHOST:localhost}:${MYSQLPORT:3306}/${MYSQLDATABASE:ash}?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:JANASHLEEBUHAWE}

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# App Settings
app.base-url=${APP_URL:http://localhost:8080}

# Abstract API (email validation)
abstract.api.key=${ABSTRACT_EMAIL_KEY:}
abstract.api.url=https://emailverification.abstractapi.com/v1/

# Gmail SMTP (local development only)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=janashleebuhawe@gmail.com
spring.mail.password=${SPRING_MAIL_PASSWORD:pstpttvgvvhjyerd}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.enable=false
spring.mail.properties.mail.smtp.connectiontimeout=30000
spring.mail.properties.mail.smtp.timeout=30000
spring.mail.properties.mail.smtp.writetimeout=30000

# Brevo REST API (Railway production only)
# Leave empty on local \u2014 EmailService auto-detects and uses Gmail SMTP instead
brevo.api.key=${BREVO_API_KEY:}
brevo.sender.email=janashleebuhawe@gmail.com
brevo.sender.name=BUHAWE


spring.jpa.properties.hibernate.hbm2ddl.halt_on_error=false

